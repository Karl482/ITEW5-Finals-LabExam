{
  "name": "sports-pwa/task-manager",
  "description": "Sports-themed Progressive Web Application for task management",
  "type": "project",
  "require": {},
  "require-dev": {},
  "scripts": {
    "dev": [
      "npm run dev"
    ],
    "install-deps": [
      "npm run install:all"
    ],
    "build": [
      "npm run build --prefix client"
    ]
  }
}
